---
applications:
- name: foo
  buildpacks:
   - dotnet_core_buildpack
  memory: 128M
  env:
    ASPNETCORE_ENVIRONMENT: development
    DOTNET_CLI_TELEMETRY_OPTOUT: true
    DOTNET_NOLOGO: true
  services:
   - myConfigServer
