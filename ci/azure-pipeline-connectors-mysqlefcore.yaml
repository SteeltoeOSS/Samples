trigger:
  branches:
    include:
    - main
    - latest
    - pr-build-changed
  paths:
    include:
    - .editorconfig
    - .gitattributes
    - .gitignore
    - ci/templates
    - Connectors/src/MySqlEFCore
    - Connectors/src/*.sln*

variables:
  - group: 'PCFone Credentials'

jobs:
  - template: templates/cloud-foundry-job.yaml
    parameters:
      feature: Connectors
      sample: MySqlEFCore
