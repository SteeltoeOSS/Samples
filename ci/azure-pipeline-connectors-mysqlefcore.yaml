trigger:
  branches:
    include:
    - main
  paths:
    exclude:
    - /*
    include:
    - Connectors/src/MySqlEFCore
pr:
  paths:
    exclude:
    - /*
    include:
    - Connectors/src/MySqlEFCore

variables:
  - group: 'PCFone Credentials'

jobs:
  - template: templates/cloud-foundry-job.yaml
    parameters:
      feature: Connectors
      sample: MySqlEFCore
