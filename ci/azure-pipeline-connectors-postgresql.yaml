trigger: none
pr:
  paths:
    include:
    - Connectors/src/PostgreSql
    exclude:
    - '/*'

variables:
  - group: 'PCFone Credentials'

jobs:
  - template: templates/cloud-foundry-job.yaml
    parameters:
      feature: Connectors
      sample: PostgreSql
