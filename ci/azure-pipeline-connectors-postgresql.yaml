trigger:
  branches:
    include:
    - main
  paths:
    exclude:
    - /*
    include:
    - Connectors/src/PostgreSql
pr:
  paths:
    exclude:
    - /*
    include:
    - Connectors/src/PostgreSql

variables:
  - group: 'PCFone Credentials'

jobs:
  - template: templates/cloud-foundry-job.yaml
    parameters:
      feature: Connectors
      sample: PostgreSql
