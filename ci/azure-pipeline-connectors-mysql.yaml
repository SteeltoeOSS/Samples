trigger:
  branches:
    include:
    - main
    - latest
    - pr-build-changed
  paths:
    include:
    - Connectors/src/MySql

variables:
  - group: 'PCFone Credentials'

jobs:
  - template: templates/cloud-foundry-job.yaml
    parameters:
      feature: Connectors
      sample: MySql
