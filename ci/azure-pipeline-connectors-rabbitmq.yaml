trigger:
  branches:
    include:
    - main
    - pr-build-changed
  paths:
    include:
    - Connectors/src/RabbitMQ

variables:
  - group: 'PCFone Credentials'

jobs:
  - template: templates/cloud-foundry-job.yaml
    parameters:
      feature: Connectors
      sample: RabbitMQ
