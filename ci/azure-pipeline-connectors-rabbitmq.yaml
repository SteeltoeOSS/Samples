trigger:
  branches:
    include:
    - main
  paths:
    exclude:
    - /*
    include:
    - Connectors/src/RabbitMQ
pr:
  paths:
    exclude:
    - /*
    include:
    - Connectors/src/RabbitMQ

variables:
  - group: 'PCFone Credentials'

jobs:
  - template: templates/cloud-foundry-job.yaml
    parameters:
      feature: Connectors
      sample: RabbitMQ
