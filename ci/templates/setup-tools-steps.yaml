steps:
- task: UsePythonVersion@0
  displayName: 'Setup Python'
  inputs:
    versionSpec: 3.11
# Ruby is only needed as a dependency for UAAC
# - task: UseRubyVersion@0
#   displayName: 'Setup Ruby'
#   inputs:
#     versionSpec: 3.1.x
