---
applications:
- name: jwtauth
  buildpacks:
   - hwc_buildpack
  memory: 256M
  stack: windows
  env:
     ASPNETCORE_ENVIRONMENT: Development
     DOTNET_CLI_TELEMETRY_OPTOUT: "true"
     DOTNET_NOLOGO: "true"
services:
   - mySSOService
