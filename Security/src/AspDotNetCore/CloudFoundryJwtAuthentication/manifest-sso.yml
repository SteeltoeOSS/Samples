---
applications:
- name: jwtauth
  buildpack: dotnet_core_buildpack
  memory: 256M
  command: cd ${HOME} && ./CloudFoundryJwtAuthentication --server.urls https://0.0.0.0:${PORT}
  env:
    ASPNETCORE_ENVIRONMENT: Development
  services:
   - mySSOService
