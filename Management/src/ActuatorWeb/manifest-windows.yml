---
applications:
- name: actuator-web-management-sample
  buildpacks:
   - binary_buildpack
  memory: 256M
  stack: windows
  command: .\actuatorweb
  env:
    DOTNET_CLI_TELEMETRY_OPTOUT: "true"
    DOTNET_NOLOGO: "true"
