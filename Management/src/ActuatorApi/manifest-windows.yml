---
applications:
- name: actuator-api
  buildpacks:
   - binary_buildpack
  memory: 256M
  stack: windows
  command: .\actuatorapi
  env:
    DOTNET_CLI_TELEMETRY_OPTOUT: "true"
    DOTNET_NOLOGO: "true"
