{
  // Steeltoe: Add schema to get auto-completion.
  "$schema": "https://steeltoe.io/schema/v4/schema.json",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Steeltoe: to use multiple databases in Cloud Foundry, uncomment the line below and add your services to manifest.yml
  //"useMultipleDatabases": true
}
