{
  // Steeltoe: Add schema to get auto-completion.
  "$schema": "https://steeltoe.io/schema/latest/schema.json",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Steeltoe: Set SQL Server connection string for non-cloud local development.
  "Steeltoe": {
    "Client": {
      "SqlServer": {
        "Default": "Server=(localdb)\\mssqllocaldb;Database=SteeltoeEFCore;Pooling=true"
      }
    }
  }
}
